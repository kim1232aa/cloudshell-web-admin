# base proxy
TUNNEL_HOST=gcs.example.com
INTERVAL=600
PROBE_PROXY=
PROBE_RETRIES=4
KEEPALIVE=1
KEEPALIVE_INTERVAL=1500

# web-admin
ADMIN_DOMAIN=admin.example.com
# generate with: python3 web_admin/hash_password.py
ADMIN_PASSWORD_HASH=
# generate with: python3 -c "import secrets; print(secrets.token_hex(32))"
SESSION_SECRET=
